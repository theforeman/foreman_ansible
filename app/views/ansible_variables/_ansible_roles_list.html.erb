<% if f.object.is_a? ::AnsibleVariable %>
  <%=
    select_f(
      f,
      :ansible_role_id,
      [f.object.ansible_role],
      :id,
      :to_label,
      {},
      { :label => _("Ansible Role"), :disabled => true }
    )
  %>
<% else %>
  <%= show_puppet_class(f) %>
<% end %>
